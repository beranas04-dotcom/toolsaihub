import AuthClient from "./AuthClient";

export const metadata = {
    title: "Sign In / Sign Up - AIToolsHub",
    description: "Access your AIToolsHub account.",
};

export default function AuthPage() {
    return <AuthClient />;
}
